import autoformatOptions from "./pluginOptions/autoformatOptions";
import listOptions from "./pluginOptions/listOptions";

const CONFIG = {
  list: listOptions,
  autoformat: {
    options: autoformatOptions
  }
};

export default CONFIG;
